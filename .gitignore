/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
/.zed
*.lock

# Dispatch bridge file. Derived from the canonical tasks DB; written on every
# `dispatch:pull` / `dispatch:push`. The DB on production is the source of truth.
# Force-commit with `git add -f docs/tasks.jsonld` if you ever want a frozen
# snapshot in history.
/docs/tasks.jsonld
